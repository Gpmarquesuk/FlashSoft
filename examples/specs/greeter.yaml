feature: "add greeter util and CLI"
acceptance:
  - running python -m src.greeter --name Ana prints "Hello, Ana!"
  - pytest passes
changes:
  summary: "create src/greeter.py with greet(name) and CLI; update README"
  constraints:
    style: "pep8"
    min_coverage: 0.0
notes: |
  Keep it simple. No external deps. Provide full file contents (not diffs).
